<p><strong>Version ID:</strong> <%= @version.id %></p>
<p><strong>Event:</strong> <%= @version.event %></p>
<p><strong>Created At:</strong> <%= format_date @version.created_at %></p>
<p><strong>Author:</strong> <%= @version.whodunnit %></p>

<br>

<div>
    <strong>Name: </strong>
    <span><%= @school_year.name %></span>
</div>

<div>
    <strong>Start Date: </strong>
    <span><%= @school_year.start %></span>
</div>

<div>
    <strong>End Date: </strong>
    <span><%= @school_year.end %></span>
</div>


<footer class="mt-4">
    <%= button_to "Rollback", admin_classes_class_sy_rollback_path(@version.id), class: "btn btn-primary", form: { data: { 'turbo-confirm': 'Are you sure you want to rollback to this version? This will overwrite the current state.', btn_class: "btn-primary" } } %>
</footer>

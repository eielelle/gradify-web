<div id="list_teachers">
  <header class="flex justify-between items-center mb-6">
    <%= search_form_for @q, url: admin_classes_class_sy_teachers_manage_index_path, method: :get, html: { class: "join" } do |f| %>
      <%= f.search_field :name_or_email_cont, placeholder: "Search", class: "input input-bordered w-96 join-item", id: "search_input" %>
      <%= f.select :s, options_for_select(@sort_fields, selected: params.dig(:q, :s)), {}, { class: "select select-bordered join-item", onchange: 'this.form.submit();' } %>
      <button type="submit" class="btn btn-primary join-item">Search</button>
    <% end %>
  </header>

  <section>
    <div class="overflow-x-auto">
      <%= form_with url: admin_classes_class_sy_teachers_manage_index_path do %>

        <table class="table table-lg mb-6">
          <!-- Table head -->
          <thead>
            <tr>
              <th>
                <label>
                  <input type="checkbox" class="checkbox" id="select_all" />
                </label>
              </th>
              <th>Name</th>
              <th>Email</th>
              <th>Created At</th>
              <th>Updated At</th>
            </tr>
          </thead>
          <tbody>
            <% if @users.present? %>
              <% @users.each do |teacher| %>
                <tr class="hover">
                  <th>
                    <label>
                      <%= check_box_tag 'teacher_ids[]', teacher.id, false, class: "checkbox" %>
                    </label>
                  </th>
                  <td><%= teacher.name %></td>
                  <td><%= teacher.email %></td>
                  <td><%= teacher.created_at %></td>
                  <td><%= teacher.updated_at %></td>
                </tr>
              <% end %>
            <% else %>
              <tr>
                <td colspan="5">No teachers found.</td>
              </tr>
            <% end %>
          </tbody>
        </table>
        
        <button type="submit" class="btn btn-primary">Assign Selected</button>
      <% end %>

      <div class="flex justify-between items-center">
        <span></span>
        <%= paginate @users, views_prefix: 'shared' %>
      </div>
    </div>
  </section>
</div>

<%= render "shared/toast", toast: flash[:toast] %>

<script>
  document.addEventListener('turbo:load', function () {
    const selectAllCheckbox = document.getElementById('select_all');
    const checkboxes = document.querySelectorAll('input[type="checkbox"].checkbox');

    selectAllCheckbox.addEventListener('change', function () {
      checkboxes.forEach(checkbox => {
        checkbox.checked = selectAllCheckbox.checked;
      });
    });
  });
</script>
